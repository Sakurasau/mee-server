enum ChatType {
  DIRECT // personal messages
  GROUP
  CHANNEL
}

enum MessageType {
  TEXT
  // инфо: звонок, системное...

  // IMAGE
  // VIDEO
  // AUDIO
  // FILE
}

enum MessageStatusType {
  SENT
  RECEIVED
  READ
}
